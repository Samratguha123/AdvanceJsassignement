// . Use slice() and substring() to extract the middle 3 characters of a given string. 
let str="ABCDE";
let ans=str.slice(1,str.length-1);
let ans1=str.substring(1,str.length-1);
console.log(ans);
console.log(ans1);
