// Write a program that extracts specific properties (name, age) from an object using destructuring. 
let obj={
    name:"samrat",
    age:23,
    roll:22,
    gender:"male"
}

let {name,age}=obj;
console.log(name);
console.log(age);
