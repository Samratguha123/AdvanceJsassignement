// Use the ternary operator to classify a student’s grade based on their score. 

const number=30;

const char=number>=80 ? 'A':(number>=70 ?'B':(number>=60)?'C':(number>=50)?'D':'FAIL')
console.log(char);
