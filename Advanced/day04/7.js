// Write a program to iterate through an object and print all key-value pairs using for...in. 

let obj={
    name:"samrat"
}

for(let a in obj){
    console.log(a);
    console.log(obj[a]);
    
}